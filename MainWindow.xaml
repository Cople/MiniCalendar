<Window x:Class="MiniCalendar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MiniCalendar.Controls"
        Title="Mini Calendar" 
        Height="470" Width="360"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Icon="pack://application:,,,/AppIconWithBg.ico"
        WindowStartupLocation="Manual">
    
    <Grid>
        <Border x:Name="MainBorder" CornerRadius="8" Background="{Binding Background, RelativeSource={RelativeSource AncestorType=Window}}">
            <controls:CalendarControl x:Name="CalendarControl"/>
        </Border>
    </Grid>
</Window>