<Window x:Class="MiniCalendar.Controls.EventDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:MiniCalendar.Controls"
        xmlns:emoji="clr-namespace:Emoji.Wpf;assembly=Emoji.Wpf"
        Title="事件详情" 
        Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}"
        WindowStyle="SingleBorderWindow"
        Icon="pack://application:,,,/AppIconWithBg.ico"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        </Style>
        <Style TargetType="emoji:TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
        </Style>
    </Window.Resources>
    
    <Border Background="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- 标题 -->
            <emoji:TextBlock x:Name="TitleText" 
                      Grid.Row="0" 
                      FontSize="20" 
                      FontWeight="Bold"
                      Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"
                      TextWrapping="Wrap"
                      Margin="0,0,0,15"/>
            
            <!-- 事件详情 -->
            <StackPanel Grid.Row="1" Margin="0,0,0,15">
                <!-- 时间 -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="时间:" FontWeight="Bold" Width="50" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <TextBlock x:Name="TimeText" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                </StackPanel>
                
                <!-- 地点 -->
                <StackPanel x:Name="LocationPanel" Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="地点:" FontWeight="Bold" Width="50" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <emoji:TextBlock x:Name="LocationText" TextWrapping="Wrap" MaxWidth="300" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                </StackPanel>
                
                <!-- 来源 -->
                <StackPanel Orientation="Horizontal" Margin="0,5">
                    <TextBlock Text="来源:" FontWeight="Bold" Width="50" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                    <TextBlock x:Name="SourceText" Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
                </StackPanel>
            </StackPanel>
            
            <!-- 描述 -->
            <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
                <emoji:TextBlock x:Name="DescriptionText" 
                          TextWrapping="Wrap" 
                          Foreground="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}"/>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>